1) read_file.py : reads the dataset and store it in the .csv file.
2) limit_dataframe.py : processing the data_set.csv to exclude the papers.
3) training_phase.py : builds and trains the model.
4) evaluation.py : evaluation of the model.
5) lstm_model.h5 : the model
6) layer1_weights.txt : the weights of layer 1
7) layer2_weights.txt : the weights of layer 2
8) layer3_weights.txt : the weights of layer 3
9) data_set.csv : data set
10) inputs.csv : limited data set
11) report.pdf
